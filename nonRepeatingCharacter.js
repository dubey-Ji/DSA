let A = "abadbc";
let B = "";

for(let i = 0; i < A.length; i++) {
    for(let j = 0; j <= i; j++) {
        if (B.length <= 0) {
            B += A[j];
        } else {
            let c = 
        }
    }
}
console.log(B);